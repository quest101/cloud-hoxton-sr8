server:
  port: 7001
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
          git:
              uri: http://git.oschina.net
              searchPaths: spring_cloud_in_action/config-repo
              username: username
              password: password
